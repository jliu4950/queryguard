# Demo mode is the default and needs no key. CI uses only this value.
LLM_PROVIDER=demo
DATABASE_URL=sqlite:///./queryguard.db
MAX_RESULT_ROWS=100

# Required only with LLM_PROVIDER=openai_compatible. Never commit real values.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
